/*#pragma once
#include "Engine.h"
#include "Mesh.h"

class Objeto
{
public:
    Objeto(ID3D11Device* device);
    ~Objeto();

    void Update(float deltaTime);
    void Render(ID3D11DeviceContext* context);

private:
    Mesh m_mesh;
    XMFLOAT3 m_position;
    XMFLOAT3 m_rotation;
    XMFLOAT3 m_scale;
};
*/





